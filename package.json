{
  "name": "halal-inventory-tracker",
  "version": "1.0.0",
  "scripts": {
    "generate": "node generate-pages.js",
    "build": "npm run generate"  // <-- THIS IS THE CRITICAL LINE
  },
  "dependencies": {
    "csv-parser": "^3.0.0"
  }
}